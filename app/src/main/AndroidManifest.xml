<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindMySpot"
        tools:targetApi="31">
        <activity
            android:name=".activities.AddCreditCard"
            android:exported="false" />
        <activity
            android:name=".activities.ReservarLugar"
            android:exported="false" />
        <activity
            android:name=".activities.NoInternet"
            android:exported="false" />
        <activity
            android:name=".activities.NewQR"
            android:exported="false"
            android:theme="@style/Theme.FindMySpot" />
        <activity
            android:name=".activities.Home"
            android:exported="false"
            android:theme="@style/Theme.FindMySpot" />
        <activity
            android:name=".auth.Register"
            android:exported="false"
            android:theme="@style/Theme.FindMySpot" />
        <activity
            android:name=".activities.MetodosPago"
            android:exported="false"
            android:theme="@style/Theme.FindMySpot" />
        <activity
            android:name=".auth.Login"
            android:exported="true"
            android:theme="@style/Theme.FindMySpot">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>